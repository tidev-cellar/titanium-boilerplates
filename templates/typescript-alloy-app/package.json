{
  "name": "typescript-app",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "ios": "appc run -p ios",
    "android": "appc run -p android",
    "lint": "tslint -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/titanium": "^7.1.0",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1"
  }
}
